name: hexcolorname_example
description: Demonstrates how to use the hexcolorname plugin.
publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  hexcolorname:
    path: "../.."

flutter:
  uses-material-design: true
